{
  "name": "do-an-tot-nghiep",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently -n BE,FE -c blue,green \"npm run dev --prefix back-end\" \"npm run dev --prefix front-end\"",
    "install:all": "npm install --prefix back-end && npm install --prefix front-end",
    "db:up": "docker compose up -d",
    "db:down": "docker compose down",
    "db:logs": "docker compose logs -f mongodb"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
